/// <reference types="express-serve-static-core" />
import * as functions from 'firebase-functions';
import { ApiConfig, createBareExpressApp } from './server';
export declare type Trigger = functions.TriggerAnnotated & ((req: Express.Request, resp: Express.Response) => void);
/**
 * Configure the Cloud Function Trigger based on options. The configuration
 * allows you to set cache expiration, enable cors, enable compression,
 * set the Firebase App instance name, and set a local port for testing. The
 * local port should not be used in production.
 * @param config
 */
export declare const trigger: (config?: ApiConfig | undefined) => Trigger;
export declare const app: typeof createBareExpressApp;

// Make Comments Iterator

function cycle_through_comments(some_nested_array) {
	// Do the nested array cycling shit
	// If the comments array is 0,
	// Move to the previous level and go to the next comments array
	// If the comments array isn't 0
	// Loop through the array and print the comments
	// Then just do that recursively until it's all done 
}

// Make Comments sub-thread counter

function compute_comments_amounts(comments_array) {
	// Do the comments shit
}
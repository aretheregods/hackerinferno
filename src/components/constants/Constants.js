import { createVNode } from 'inferno';
import { DataContainer } from '../data/Data-Container';
import { HtmlElement } from 'inferno-vnode-flags';

export const icv = createVNode;
export const DC = DataContainer;
export const he = HtmlElement
